// add scripts

$(document).on('ready', function() {
  console.log('sanity check!');
  var inputBtn = $('.input__input');

  inputBtn.on('keyup', function() {
    console.log(inputBtn.html());
    // $inputBtn.empty();
  });

});




//make an object with all the images and corresponding letter/numbe value

//dom onkeydown display image in main focus box, add image to footer

//dom on click reset button, clear footer

//full alphabet view?
